import { FakeApiInterceptor } from './fake-api-interceptor';

describe('FakeApiInterceptor', () => {
  it('should create an instance', () => {
    expect(new FakeApiInterceptor()).toBeTruthy();
  });
});
